# Copy to .env.local. These are the PUBLIC (anon) values — safe in the browser.
# Row-level security (schema.sql) keeps writes locked to the service key.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key...
